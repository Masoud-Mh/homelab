// Vitest setup: adds jest-dom matchers (e.g. toBeInTheDocument) to vitest's expect.
// The /vitest entry extends vitest's expect directly (no `globals: true` needed).
import '@testing-library/jest-dom/vitest'
